# Frontend Environment Variables
# Copie para .env.local e ajuste conforme necessário

# URL da API backend (vazio = mesmo domínio via proxy Next.js rewrite)
# Em desenvolvimento local sem Docker: http://localhost:8000
# Em Docker: vazio (usa rewrite para http://backend:8000)
NEXT_PUBLIC_API_URL=
